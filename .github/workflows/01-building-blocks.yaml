name: 01-building-blocks
on: push
jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: say hellow
              run: echo "Hello, World!"
    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: Failed step
              run: echo "I will fail"
            - name: Say goodbye
              run: "Goodbye!"
